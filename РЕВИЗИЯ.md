# Ревизия Cappi Core — 11.09.2026

Полный проход: код-ревью на мёртвый код и хвосты, краш-тест, проверка
всех команд и кнопок, аудит расчётов. Ниже — что нашлось и что с этим
сделано. Всё, что помечено «исправлено», проверено запуском на живых
данных, а не только чтением кода.

## Как проверялось

| Что | Объём | Результат |
|---|---|---|
| Статический анализ | 12 файлов, 3 100 строк | 8 мёртвых имён, 2 лишних импорта, 1 дубль определения |
| Краш-тест | 59 текстовых вводов, 40 кнопок | было 17 падений → стало 0 |
| Команды и экраны | 18 команд, 43 экрана, 13 кнопок | 74 проверки, 0 падений, 0 молчаний |
| Сверка расчётов | пул 2%, KPI, времена | 2 ошибки в деньгах |

---

## Критическое

### 1. Все экраны KPI падали, как только вносились тайники
`_знак` был определён в `bot.py` дважды: старая версия для KPI
(`факт, норма, меньше_лучше`) и новая для «Времени работы»
(`факт, цель`). Второе определение молча перекрывало первое, и вызов
`_знак(проц, норма, False)` падал с `TypeError`.

Ломалось не всегда, а ровно тогда, когда в тайниках появлялись данные —
то есть когда функцией начали пользоваться. Молчаливое перекрытие имени
не даёт ни ошибки импорта, ни предупреждения.

**Исправлено:** новая функция переименована в `_против_цели`.
**Проверено:** все три экрана KPI (шеф, Лазарева, Левітана) с
заполненными тайниками.

### 2. Пул кухни выходил за 2% выручки
Часы сотрудников без определённого филиала финансировались отдельным
пулом **поверх** филиальных: филиалы уже разбирали все 2%, а этим людям
начислялось сверх. За август — 9 621 ₴ лишних.

**Исправлено:** часы без филиала раскидываются по филиалам
пропорционально их часам; ставка для таких людей — средняя по сети,
взвешенная по часам.
**Проверено:** сумма всех выплат минус 2% выручки = 0,00 ₴.

### 3. Премия начислялась по цифрам, которых никто не видел
Если Loopa не отвечала, негатив считался нулём, критерий «выполнен», и
1 250 ₴ уходили сотруднику. Сломанная интеграция выглядела как отличный
результат.

**Исправлено:** нет данных ≠ выполнено. В отчёте отдельная строка:
«данных нет, не зачтено — это не оценка работы, проверь источник».

---

## Падения на вводе (было 17, стало 0)

| Ввод | Было | Стало |
|---|---|---|
| сообщение из одних пробелов | `ValueError` на распаковке | молча игнорируется |
| `/report абв`, `/report 2026-13-45` | `ValueError` | «Не понял дату», с примером |
| `/set 03275 46 32.13` | `ValueError: month must be in 1..12` | «Не понял дату» |
| `zc:абв`, `zd:x:y`, `zk:абв`, `zo:` | `ValueError` на `int()` | «Кнопка устарела» |
| `bd:абв`, `bs:x:чушь` | `ValueError` | «Кнопка устарела» |
| `ag:999:чушь` | `ValueError: неизвестная роль` | «Кнопка устарела» |
| `pc:чушь`, `km:чушь:чушь` | `ValueError` на дате | «Кнопка устарела» |

Общая причина одна: пользовательский ввод и аргументы кнопок разбирались
напрямую через `int()` и `date.fromisoformat()`. Кнопка живёт в чате
вечно — нажатую вчера жмут сегодня, старый экран открывают после
обновления. Добавлены `_номер()` и `дата_из_текста()`; ни один разбор
чужих данных больше не идёт напрямую.

---

## Мёртвый код и хвосты

| Что | Решение |
|---|---|
| `cappi.site_prices_by_name` — не вызывается ниоткуда | удалено |
| `jamshut.можно_управлять` — написано и не использовалось | подключено: без ключа на запись бот честно отказывает вместо ошибки 404 |
| `bugs.подчистить` — написано и не вызывалось, журнал рос вечно | вызывается раз в сутки из фонового цикла |
| `import re` в `aliases.py`, `import urllib.error` в `jamshut.py` | удалены |
| `report.by_category` | оставлено: готово к экрану, помечено в коде |
| «мёртвый» success-`say` за `return` внутри `except` | исправлено ранее: подтверждение смены цены не отправлялось никогда |

---

## Производительность

| Экран | Было | Стало | Что сделано |
|---|---|---|---|
| Итоги дня | 7,5 с | 3,4 с | 10 независимых запросов параллельно |
| Проверка связи | 19,1 с | 3,3 с | 12 проверок параллельно, порядок вывода сохранён |
| Правки явок | 45,7 с | 3,2 с | кэш явок (был сделан ранее) |
| Время работы за месяц | 5,1 с | 2,9 с | 6 периодов параллельно |

Из 43 экранов ни одного дольше 5 секунд.

---

## Находки не в коде

- **Glovo: «Моршинська без газу 0,5 л» — 99 ₴ при цене 49 ₴ в Syrve.**
  Остальные 188 позиций сходятся, значит это не наценка площадки, а
  застрявшая цена. Гость платит вдвое.
- Пароли и токены, пересылавшиеся в переписке, не сменены.
- `шеф_id` не задан — запрос правок часов 2-го числа отправлять некому.

---

## Что осталось

- Копирайтинг прошёл по помощи и ключевым сообщениям; остальные экраны
  не вычитывались.
- Экран `by_category` (продажи по категориям) готов в расчётах, но не
  выведен в интерфейс.
- Приказ на конкретный филиал: сейчас цена меняется по всей сети.
